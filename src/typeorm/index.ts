import { User } from './User';

const entities = [User]; // if we get more entities we add them to this array

export { User }; // import entities independently

export default entities; // import all of them
